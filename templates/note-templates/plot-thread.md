---
type: plot-thread
name: "REPLACE: short thread name"
status: open
tier: "REPLACE: core | side | decorative"
introduced_chapter: "REPLACE: e.g. 0003"
payoff_chapter: "REPLACE: e.g. 0012, or leave blank if open-ended by design"
payoff_book: "REPLACE: only for series-spanning threads — which book in the series owes the payoff"
tags: [plot-thread]
---

<!--
Tier guide (adapted from webnovel-writer's foreshadowing taxonomy):
- core (weight 3.0): protagonist's origin, the ultimate antagonist, the
  source of their central advantage. Recovery window: 50-300 chapters.
- side (weight 2.0): a supporting character's motive, a mysterious object,
  a faction secret. Recovery window: 30-100 chapters.
- decorative (weight 1.0): a scene detail, a small habit, an offhand
  mention. Recovery window: 10-30 chapters.

Urgency = (chapters elapsed since introduced_chapter / target payoff
window for this tier) × tier weight. Status thresholds: above 1.0 (or past
payoff_chapter entirely) = critical; above 0.8 = warning; below = normal.
The Thread-Ledger Reviewer computes this — you don't need to calculate it
by hand, but knowing the formula explains why a thread got flagged.

Keep concurrently-open threads to roughly 5 or fewer — more than that and
readers (and reviewers) lose track of what's actually being tracked.
-->


# {{name}}

## Setup

<!-- What was established, and how — quote or closely paraphrase the
chapter's actual text, not a vague gesture at "there's a mystery about X." -->

## Payoff (fill in when resolved)

<!-- What actually resolves it, and whether it answers the same question
the setup raised — a payoff that answers a different question than the one
posed is itself a Thread-Ledger issue, not a completed thread. -->

## Involved

<!-- Wikilinks to characters/world elements this thread involves. -->
